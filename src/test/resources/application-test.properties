# --- AWS/S3 ? (S3Config @Profile("!test")? ????? ???) ---
spring.cloud.aws.s3.enabled=false
spring.cloud.aws.stack.auto=false

# --- ? ??? ?????? ???? ??? ?? ---
# (???? @Value("${SPRING_CLOUD_AWS_S3_BUCKET:local-test-bucket}")?? ???? ?? ? ??)
SPRING_CLOUD_AWS_S3_BUCKET=local-test-bucket

# --- ?? ? ? (?? ?) ---
ml.server.url=http://localhost:5000
ml.server.endpoint=/analyze-emotion

# --- JWT Secret Key for testing ---
JWT_SECRET_KEY=test-secret-key-for-jwt-testing-only